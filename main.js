/** @type {Product} */
const product = { name: "Telefon", price: 222 };

/**
 * Beskriv funktionen här
 * @param {string} color beskriv vad color är
 * @param {Product} product beskriv vad product är
 * @param {boolean} third beskriv var third är
 * @return {string} beskriv vad funktionen returnerar
 */
function doSomethingWithOurProduct(color, product, third) {
  console.log('hej');
  return 'Hej då';
}

// Hoppa ord för ord
// alt + ->
// alt + <-

// Hoppa till slutet/början
// cmd + ->
// cmd + <-

// Markera tecken för tecken
// shift + ->
// shift + <-

// Markera ord för ord
// shift + alt + ->
// shift + alt + <-

// Flyttar raden/raderna
// alt + ^
// alt + v

// Kopierar raden/raderna
// shift + alt + ^
// shift + alt + v